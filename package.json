{
  "name": "payment-form",
  "version": "0.0.1",
  "scripts": {
    "dev": "esbuild src/main.js --bundle --outdir=dist --format=esm --sourcemap --loader:.html=text --watch --servedir=.",
    "build": "esbuild src/main.js --bundle --outdir=dist --format=esm --minify --loader:.html=text",
    "preview": "npx serve dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:run": "vitest run"
  },
  "type": "module",
  "homepage": "https://namewiz.github.io/payment-form",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/namewiz/payment-form.git"
  },
  "bugs": {
    "url": "https://github.com/namewiz/payment-form/issues"
  },
  "author": "Justice Ogbonna <dev@justiceo.com>",
  "keywords": [
    "domain-registration",
    "domain-payment",
    "standalone-payment-component"
  ],
  "license": "MIT",
  "devDependencies": {
    "jsdom": "^25.0.1",
    "esbuild": "^0.24.0",
    "vitest": "^3.2.4"
  }
}
